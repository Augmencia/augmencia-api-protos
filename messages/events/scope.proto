syntax = "proto3";

package Augmencia.Protos.Events;

import "messages/ar/contexts-ids-with-project-id.proto";

message Scope {
  bool organizations = 1;
  bool projects = 2;
  repeated string assets = 3;
  repeated AR.ContextsIdsWithProjectId ar_assets = 4;
  repeated string data = 5;
}
