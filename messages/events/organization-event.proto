syntax = "proto3";

package Augmencia.Protos.Events;

import "messages/organizations/organization.proto";

message OrganizationEvent {
    oneof event {
        Organizations.Organization added = 1;
        Organizations.Organization modified = 2;
        string deleted = 3;
    }
}
